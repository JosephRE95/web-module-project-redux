const inistialState = {
    favorits: [],
    displayFavorites: true
}

const favoriteReducer = (state = inistialState, action)=> {
    switch(action.type){
        default:
            return(state);
    }
}

export default favoriteReducer;